# Optional: Qwen (DashScope compatible mode)
DASHSCOPE_API_KEY=
QWEN_MODEL=qwen-plus

# Protocol OS + workflow (Prisma + Postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stochstack?schema=public"
